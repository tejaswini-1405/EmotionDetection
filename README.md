echo "# Emotion Sense"
A Flask web app for real-time emotion detection using DeepFace and a custom CNN model.

## Features
- Image-based emotion detection
- Live webcam emotion detection
- Two models: DeepFace and Custom CNN

## Run
pip install flask deepface opencv-python tensorflow keras numpy
python app.py
" > README.md
git add README.md
git commit -m "Add README"
git push
